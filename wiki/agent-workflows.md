---
title: Agent Skills & Workflows
generated: 2026-08-29
generator: claude-sonnet-5
sources:
  - .agents/skills/README.md
  - .agents/skills/commit-message/SKILL.md
  - .agents/skills/diagram-archimate/SKILL.md
  - .agents/skills/diagram-c4/SKILL.md
  - .agents/skills/diagram-plantuml/SKILL.md
  - .agents/skills/fork-sync/SKILL.md
  - .agents/skills/model-archimate/SKILL.md
  - .agents/skills/speckit-analyze/SKILL.md
  - .agents/skills/speckit-checklist/SKILL.md
  - .agents/skills/speckit-clarify/SKILL.md
  - .agents/skills/speckit-constitution/SKILL.md
  - .agents/skills/speckit-implement/SKILL.md
  - .agents/skills/speckit-plan/SKILL.md
  - .agents/skills/speckit-specify/SKILL.md
  - .agents/skills/speckit-tasks/SKILL.md
  - .agents/skills/speckit-taskstoissues/SKILL.md
  - .agents/skills/wiki-editor/SKILL.md
  - .agents/skills/wiki-librarian/SKILL.md
  - .agents/agents/commit-auditor.md
  - .agents/agents/wiki-maintenance.md
---

> Generated by `wiki-librarian` from the sources above. Edit `wiki/sources.yaml`
> and rebuild rather than editing this page.

`.agents/skills/` is a **tool-neutral** catalog of skills — any coding agent
that supports skills exposes each by its directory name
(`.agents/skills/README.md:3-6`). Each skill is a directory with a `SKILL.md`
plus supporting files. `.agents/agents/` holds paired sub-agents.

## Spec-Kit workflow (`speckit-*`)

From [github/spec-kit](https://github.com/github/spec-kit). The skill
directories, `.specify/templates/`, `.specify/scripts/`, and
`.specify/integrations/` are **git-ignored** and regenerated by
`.devcontainer/post-create.sh` against the `specify-cli` release current at
container-create time; the pinned version lives in `.specify/speckit.lock`
(tracked). `.specify/memory/` (the constitution) is hand-owned and never
regenerated (`.agents/skills/README.md:11-19`).

| Skill | Purpose (`.agents/skills/README.md:21-31`) |
|---|---|
| `speckit-constitution` | Project constitutional rules, quality gates, non-functional requirements |
| `speckit-specify` | Craft or refine a feature specification when requirements change |
| `speckit-clarify` | Resolve unanswered questions before design or coding |
| `speckit-plan` | Generate / update the implementation plan (phases, design docs, output) |
| `speckit-tasks` | Turn a spec into executable, dependency-ordered tasks |
| `speckit-analyze` | Survey specs, code, and tasks to scope a feature |
| `speckit-checklist` | Verify checklist completion and gate entry into implementation |
| `speckit-implement` | Execute the implementation phase once design and planning are solid |
| `speckit-taskstoissues` | Translate task lists into issue-tracker-friendly format |

These realize the Specify → Plan → Implement → Verify workflow from
[Governance & Constitution](governance-and-constitution.md).

## Repo maintenance

### `commit-message` (+ `commit-auditor` agent)

Writes and checks git commit messages against
[Conventional Commits](https://www.conventionalcommits.org/en/v1.0.0/) and this
repo's conventions, enforced through commitizen (`[tool.commitizen]` in
`pyproject.toml`) (`.agents/skills/commit-message/SKILL.md:7-16`). Releases and
the changelog are driven from commit history, so a malformed message is "a real
defect, not a style nit". Subject form: `<type>(<scope>)?: <description>`,
imperative, lower-case, no trailing period, ≤ 72 chars; scope is the affected
package or top-level area (`agents`, `skills`, `devcontainer`, `specs`, `ci`, …).
The authoritative ruleset is `commit-message/references/standard.md`.

**`commit-auditor`** (`.agents/agents/commit-auditor.md`) is a read-only agent
that audits every commit a branch adds over its base (default `main`) and
returns a per-commit conformance report with corrected messages — it never
amends, rebases, or pushes. Run it before opening a PR.

### `fork-sync`

Keeps forked GitHub dependencies in sync with upstream: rebases `fix/*` and
`private/*` branches onto the latest upstream, rebuilds a `dist-candidate`
branch, runs tests, flags security-relevant upstream changes, and escalates
conflicts instead of guessing (`.agents/skills/fork-sync/SKILL.md:2-3`).
`forks.yml` is the single source of truth — each fork declares its upstream and
fork URLs, tracked branch, ordered `fix_branches` / `private_branches`, an
optional `test_cmd`, and `security_paths` globs. Deterministic scripts do the
mechanical work and emit JSON + exit codes; the skill applies judgement only
where a script escalated. Runs interactively (`/fork-sync [name]`) or on a
timer via `dist/run-fork-sync.sh`. This supports the `third_party/` submodule
strategy in [Architecture Overview](architecture.md).

## Diagrams & architecture models

A dependency chain, model-first (`.agents/skills/README.md:49`):
`diagram-c4` / `diagram-archimate` → `model-archimate` (validated model) →
`diagram-plantuml` (render). `pyarchimate` is a `dev` dependency; run the skill
scripts with `poetry run python …`.

| Skill | Purpose |
|---|---|
| `model-archimate` | Author, edit, and **validate** ArchiMate models (`.archimate` Archi-native or Open Group Exchange `.xml`). Runs pyArchimate's metamodel checks — relationship-matrix legality plus referential integrity — and "is the conformance gate every ArchiMate/C4 diagram is generated from" (`.agents/skills/model-archimate/SKILL.md:3`). The model is the single source of truth. |
| `diagram-archimate` | Projects a **validated** model into ArchiMate-notation PlantUML via `scripts/model_to_puml.py` (optionally `--view "Name"`), mapping element types to `<archimate/Archimate>` macros, then hands the `.puml` to `diagram-plantuml` to render (`.agents/skills/diagram-archimate/SKILL.md`). |
| `diagram-c4` | Projects a validated model into C4-PlantUML (Context or Container) via `scripts/model_to_c4.py --system "..." --level context|container`, using the fixed mapping in `diagram-c4/references/archimate-to-c4-mapping.md`. A pure business/motivation model has no C4 projection (`.agents/skills/diagram-c4/SKILL.md`). |
| `diagram-plantuml` | Default workflow for authoring, validating, and rendering any PlantUML diagram. Validates against the local `plantuml` binary (`plantuml -checkonly -failfast2`), falls back to the public PlantUML server (noting that source leaves the machine), then to eyeballing; `plantuml.com` docs are the syntax fallback (`.agents/skills/diagram-plantuml/SKILL.md`). |

These operate on the formats in [Sample Data](sample-data.md).

## Project wiki

The skills that produced this wiki (`.agents/skills/README.md` §"Project wiki"):

| Skill / agent | Purpose |
|---|---|
| `wiki-librarian` | Build or refresh `wiki/` — a derived cache. Scaffolds `wiki/` when absent, resolves `wiki/sources.yaml` (repo globs + external URLs), diffs sources against `wiki/.build-log.yaml`, and regenerates NEW/STALE topic pages as synthesized prose with citations. Tools: `resolve_sources`, `build_status`, `verify_wiki`, `semsearch` (local-only semantic index). |
| `wiki-editor` | Formatting/consistency pass over the generated pages — heading hierarchy, citation-link and frontmatter hygiene, dead internal links — without changing technical meaning. |
| `wiki-maintenance` (agent) | Audits the generated wiki: stale pages, unmatched/uncovered `sources.yaml` globs, index links, dead citations, oversized pages. Flags for a librarian run (`.agents/agents/wiki-maintenance.md`). |

## Ephemeral skills

`.agents/skills/README.md:51-60` also lists two skills pulled fresh from
upstream on every container build and written under git-ignored
`.claude/skills/`: `caveman` (compressed output mode) and `session-handoff`
(structured handoff documents). Losing them between rebuilds is harmless.

## Integration point

`AGENTS.md` §"Skills & Workflows" points coding agents at
`.agents/skills/README.md` and says to invoke each skill by its directory name.
