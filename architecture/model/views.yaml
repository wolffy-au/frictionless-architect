# ArchiMate views — scoping for the diagram-archimate skill.
#
# Each view: id / name / (members: [element ids] and/or include_types: [types])
#            / viewpoint? / diagram?
#
# `build.py` creates the view, adds the matching element nodes, and (if
# `viewpoint:` is set and not `custom`) holds it to that standard ArchiMate
# viewpoint's allowed concepts — a stray concept fails the build. Slugs and
# allow lists:
#   .agents/skills/model-archimate/reference/archi-viewpoints.xml
#   (list them: poetry run python .agents/skills/model-archimate/scripts/viewpoints.py list)
# `viewpoint: custom` = a deliberate cross-layer view; omitting the key skips
# the check.
#
# `diagram:` is the output path (no extension) under diagrams/ used by
# `render_diagrams.py`. Vision-phase (TOGAF ADM Phase A) views render under
# diagrams/vision/.
#
# The C4 context / container diagrams are NOT views — render_diagrams.py
# projects them via diagram-c4 with --system "Frictionless Architecture Platform".

# =====================================================================
# TOGAF ADM Phase A — Architecture Vision
# =====================================================================

- id: view-stakeholder
  name: Stakeholder
  viewpoint: stakeholder
  diagram: vision/stakeholder
  # The case for change: who holds which driver, the assessments that make each
  # driver urgent, and the goal + outcome they drive toward.
  include_types: [Stakeholder, Assessment, Driver, Goal, Outcome]

- id: view-motivation
  name: Motivation
  viewpoint: motivation
  diagram: vision/motivation
  # The whole motivation model — stakeholders, assessments, drivers, goal,
  # outcome, principles, requirements, constraints — and how they relate.
  include_types:
    [Stakeholder, Assessment, Driver, Goal, Outcome, Principle, Requirement, Constraint]

- id: view-goal-realization
  name: Goal Realization
  viewpoint: goal_realization
  diagram: vision/goal-realization
  # How the goal refines into the outcome, and how principles, requirements and
  # constraints realize it.
  include_types: [Goal, Outcome, Principle, Requirement, Constraint]

- id: view-strategy
  name: Strategy
  viewpoint: strategy
  diagram: vision/strategy
  # Overview: resources -> capabilities -> courses of action, the value stream
  # as a single element, and the outcome. Stage decomposition and the
  # recipient stakeholders belong to the Value Stream view; the goal linkage
  # (Course of Action -> Goal) is out of scope here — the strategy viewpoint
  # stops at the strategy layer — see Goal Realization for that.
  include_types: [Capability, CourseOfAction, Resource]
  members:
    - vs-governed-delivery
    - outcome-executable-intelligence

- id: view-capability
  name: Capability Map
  viewpoint: capability
  diagram: vision/capability-map
  # The eight abilities and the Serving dependency order between them.
  include_types: [Capability]

- id: view-value-stream
  name: Value Stream — Governed Architecture Delivery
  viewpoint: value_stream
  diagram: vision/value-stream
  # The stream and its six stages (Composition + Triggering sequence, with the
  # Reconcile→Specify feedback Flow), the capabilities that Serve each stage,
  # the recipient stakeholders the stream delivers value to, and the Outcome it
  # Realizes. Resources and courses of action belong to the Strategy view.
  include_types: [ValueStream]
  members:
    - cap-control-catalog
    - cap-reusable-architecture
    - cap-digital-twin
    - cap-spec-engine
    - cap-control-plane
    - cap-forensic-ledger
    - cap-human-approval-workflow
    - cap-drift-dashboard
    - stk-delivery
    - stk-architecture
    - stk-risk-compliance
    - outcome-executable-intelligence

- id: view-outcome-realization
  name: Outcome Realization
  viewpoint: outcome_realization
  diagram: vision/outcome-realization
  # How business processes realize capabilities, which serve the value stream,
  # which realizes the outcome — the end-to-end realization chain.
  include_types: [Capability, ValueStream, BusinessProcess]
  members:
    - outcome-executable-intelligence

# =====================================================================
# Cross-cutting / later-phase views
# =====================================================================

- id: view-skeleton
  name: Architecture Skeleton
  diagram: frictionless-architect-skeleton
  # The load-bearing subset (ADR-0010): motivation spine + the capabilities it
  # drives. A deliberate cross-layer cut — no standard viewpoint spans
  # motivation elements and Capability together.
  viewpoint: custom
  include_types: [Driver, Goal, Outcome, Principle, Constraint, Requirement, Capability]

- id: view-delivery
  name: Delivery Choreography
  diagram: frictionless-architect-delivery
  # Business Process Cooperation in spirit, but it also shows the capabilities
  # the processes realize (not a Business Process Cooperation concept).
  viewpoint: custom
  include_types: [BusinessProcess]
  members:
    - cap-spec-engine
    - cap-control-plane
    - cap-drift-dashboard

- id: view-subsystem-capabilities
  name: Subsystems & Capabilities
  diagram: frictionless-architect-subsystem-capabilities
  # Application Component -> Strategy Capability realization: a bridge view
  # that spans the application and strategy layers by design.
  viewpoint: custom
  members:
    - sub-catalog
    - sub-library
    - sub-twin
    - sub-governance
    - sub-assurance
    - sub-modelling
    - cap-control-catalog
    - cap-reusable-architecture
    - cap-digital-twin
    - cap-forensic-ledger
    - cap-spec-engine
    - cap-control-plane
    - cap-drift-dashboard
    - cap-human-approval-workflow

- id: view-artefact-oscal
  name: Artefact Flow — Controls & OSCAL
  viewpoint: application_cooperation
  diagram: frictionless-architect-artefact-oscal
  members:
    - sub-catalog
    - fn-policy-authoring
    - fn-oscal-conversion
    - ext-regsources
    - art-policy-doc
    - art-baseline
    - art-oscal-catalog
    - art-oscal-profile
    - store-oscal

- id: view-artefact-library
  name: Artefact Flow — Library & Design
  viewpoint: application_cooperation
  diagram: frictionless-architect-artefact-library
  members:
    - sub-library
    - fn-pattern-authoring
    - fn-blueprint-authoring
    - fn-solution-compose
    - fn-threat-model
    - art-oscal-catalog
    - art-oscal-profile
    - art-oscal-component
    - art-oscal-ssp
    - art-pattern
    - art-blueprint
    - art-solution-design
    - art-threat-model
    - store-patterns
    - store-oscal

- id: view-artefact-twin-governance
  name: Artefact Flow — Digital Twin & Governance
  viewpoint: application_cooperation
  diagram: frictionless-architect-artefact-twin-governance
  members:
    - sub-twin
    - sub-governance
    - sub-library
    - sub-modelling
    - fn-twin-ingest
    - fn-ledger-record
    - fn-options-model
    - fn-governance-eval
    - fn-notation-engine
    - ext-cloud
    - ext-cicd
    - ext-rfp
    - art-infra-state
    - art-deploy-event
    - art-twin
    - art-solution-design
    - art-candidate-arch
    - art-impact-assessment
    - art-adr
    - art-archived-option
    - art-ledger-entry
    - art-metamodel
    - store-akg
    - store-ledger

- id: view-it4it-alignment
  name: IT4IT Alignment
  diagram: frictionless-architect-it4it-alignment
  # The IT4IT 3.0 value-stream skeleton only — see section D of elements.yaml
  # / relationships.yaml (props: {source: it4it}). Not yet bridged to this
  # platform's own capabilities. Deliberately excluded from every other view;
  # render/hide this one independently.
  viewpoint: value_stream
  members:
    - it4it-vs-root
    - it4it-outcome-digital-product
    - it4it-vs-explore
    - it4it-vs-evaluate
    - it4it-vs-integrate
    - it4it-vs-deploy
    - it4it-vs-release
    - it4it-vs-operate
    - it4it-vs-consume
    - it4it-explore-architecture
    - it4it-explore-refine-backlog
    - it4it-explore-prioritize-backlog
    - it4it-explore-roadmap
    - it4it-evaluate-influencers
    - it4it-evaluate-backlog-mandates
    - it4it-evaluate-governance
    - it4it-evaluate-gaps
    - it4it-evaluate-investments
    - it4it-integrate-plan-release
    - it4it-integrate-design-develop
    - it4it-integrate-build-test
    - it4it-integrate-accept-publish
    - it4it-deploy-plan-approve
    - it4it-deploy-fulfill
    - it4it-deploy-observe
    - it4it-deploy-validate
    - it4it-release-define-offer
    - it4it-release-implement-offer
    - it4it-release-publish-offer
    - it4it-operate-detect
    - it4it-operate-diagnose
    - it4it-operate-resolve
    - it4it-consume-publish-status
    - it4it-consume-support
    - it4it-consume-agree-offer
    - it4it-consume-subscribe-offer
    - it4it-consume-select-offer

- id: view-artefact-assurance-spec
  name: Artefact Flow — Assurance & Specification
  viewpoint: application_cooperation
  diagram: frictionless-architect-artefact-assurance-spec
  members:
    - sub-assurance
    - sub-modelling
    - fn-spec-generator
    - fn-enforcement-gate
    - fn-effectiveness-monitor
    - fn-drift-engine
    - ext-cicd
    - ext-scm
    - ext-itsm
    - art-pattern
    - art-blueprint
    - art-solution-design
    - art-dev-spec
    - art-oscal-ssp
    - art-oscal-catalog
    - art-release-candidate
    - art-gate-decision
    - art-effectiveness-evidence
    - art-twin
    - art-adr
    - art-drift-report
    - art-remediation-item
